services:
  backend:
    build:
      context: ./backend
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: "local"
      # Override cors.allowed-origins from application-local.properties if needed
      CORS_ALLOWED_ORIGINS: "http://localhost:5173,https://localhost"
      # Set your local timezone – e.g. Europe/Berlin, America/Sao_Paulo, America/New_York
      CLOCK_TIMEZONE: "Europe/Brussels"
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M

  frontend:
    build:
      context: ./frontend
      args:
        # Empty string → frontend uses relative /api/... URLs, proxied by nginx to backend
        VITE_API_URL: ""
    ports:
      - "80:80"
      - "443:443" # ← added HTTPS port
    volumes:
      - ./frontend/nginx/certs:/etc/nginx/certs:ro # ← mounts the certs
      - ./frontend/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
